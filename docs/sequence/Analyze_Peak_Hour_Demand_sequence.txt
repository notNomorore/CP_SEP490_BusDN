@startuml
title Analyze Peak Hour Demand - Sequence Diagram

hide footbox
skinparam sequenceMessageAlign center
skinparam responseMessageBelowArrow true
skinparam ParticipantPadding 25
skinparam BoxPadding 10

actor Admin
participant "Admin Web App" as AdminWebApp
participant "RevenueController" as RevenueController
participant "AnalyticsService" as AnalyticsService
participant "TripRepository" as TripRepository
database "Trip Collection" as TripCollection
participant "TicketRepository" as TicketRepository
database "Ticket Collection" as TicketCollection

note over RevenueController, AnalyticsService
Implemented by RevenueReportService.getPeakHourDemand at GET /api/admin/revenue/peak-hour-demand.
end note

Admin -> AdminWebApp: 1: Analyze peak hour demand
activate AdminWebApp
AdminWebApp -> RevenueController: 1.1: GET /api/admin/revenue/peak-hour-demand
activate RevenueController
RevenueController -> AnalyticsService: 1.2: getPeakHourDemand(filters, admin)
activate AnalyticsService
AnalyticsService -> TicketRepository: 1.3: load passenger and ticket events
activate TicketRepository
TicketRepository -> TicketCollection: 1.3.1: query revenue event sources
activate TicketCollection
TicketCollection --> TicketRepository: 1.3.2: passenger/ticket events
deactivate TicketCollection
TicketRepository --> AnalyticsService: 1.3.3: filtered events
deactivate TicketRepository
AnalyticsService -> TripRepository: 1.4: correlate crowded routes where available
activate TripRepository
TripRepository -> TripCollection: 1.4.1: query trip route data
activate TripCollection
TripCollection --> TripRepository: 1.4.2: trip context
deactivate TripCollection
TripRepository --> AnalyticsService: 1.4.3: trip context
deactivate TripRepository

alt [no demand data]
    AnalyticsService --> RevenueController: 1.5: empty peak hour demand
    RevenueController --> AdminWebApp: 1.6: 200 No demand data
else [demand data available]
    loop 1.5: Group events by hour and route
        AnalyticsService -> AnalyticsService: 1.5.1: calculate passengers and revenue per hour
        activate AnalyticsService
        AnalyticsService --> AnalyticsService: 1.5.2: hourly demand row
        deactivate AnalyticsService
    end
    AnalyticsService --> RevenueController: 1.6: peak hours and crowded routes
    RevenueController --> AdminWebApp: 1.7: 200 Peak hour demand
end
deactivate AnalyticsService
deactivate RevenueController
AdminWebApp --> Admin: 1.8: Display demand analysis
deactivate AdminWebApp

@enduml
